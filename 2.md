好的，我将为你撰写一份详尽的研究报告，围绕下行MISO系统中基于单层RSMA架构下，结合平均信息年龄（AoI）、URLLC需求（BLER与时延）、资源优化（功率、速率、块长、调度）及信道不确定性的多模型问题建模。报告将包括：系统建模图、关键变量定义、约束推导、优化目标函数形式，以及适用的多种求解策略（如凸优化与深度强化学习），并附带公式推导与参考文献标注。
我完成后会通知你查阅。


# 基于单层RSMA的下行MISO系统平均AoI最小化资源优化建模（满足URLLC约束）

## 引言

在未来无线网络中，\*信息年龄（Age of Information, AoI）\*已经成为衡量信息传递实时性的重要指标。降低平均AoI对于时延敏感应用（如自动驾驶、远程医疗等）至关重要。同时，*超可靠低时延通信（URLLC）*场景要求传输具有极高可靠性和极低时延，这通常需要在**短包通信**和\*\*有限块长（Finite Blocklength, FBL）\*\*条件下实现极低的误块率（*块错误率*, BLER）约束。传统多址接入方案如正交多址（OMA）或完全干扰抵消的功率域NOMA在这方面存在性能局限。近年来，*速率拆分多址（Rate-Splitting Multiple Access, RSMA）*被提出作为下一代多天线系统中一种灵活高效的传输策略。RSMA通过将用户消息拆分为*公共部分*和*私有部分*进行编码和线性预编码传输，能够在部分解码干扰与将干扰视作噪声之间灵活折中干扰管理。这使其在各干扰强度场景下都能高效工作，并包含SDMA和NOMA等方案作为特例。尤其值得关注的是，RSMA在6G背景下被证明可以显著提升系统性能，满足增强型URLLC和eMBB等多种业务的需求。研究表明，与NOMA和SDMA相比，RSMA能够在过载网络中以更小的块长实现相同吞吐量，从而大幅降低传输时延。基于以上优势，本文聚焦**下行MISO（发射端多天线，接收端单天线）系统**采用单层RSMA策略，在满足URLLC短包高可靠传输约束的同时，对资源优化建模以**最小化平均信息年龄**。下文将详细介绍系统架构、优化变量及约束条件，给出平均AoI的计算公式和优化目标，在不同信道CSI假设下建立模型，并讨论求解该非凸优化问题的有效方法。

## 系统模型与RSMA架构

我们考虑一个下行MISO广播信道，基站配备了$N_t$根发射天线，服务$K$个单天线用户。基站采用**单层RSMA**策略，即每个用户$k$的消息$W_k$被拆分成*公共部分*$W_{c,k}$和*私有部分*$W_{p,k}$。将所有用户的公共部分组合形成公共消息$W_{c}$，编码为单独的公共码流$s_{c}$；每个用户的私有部分独立编码为私有码流$s_{k}$。公共码流和所有私有码流经线性预编码后进行超叠加并同时发送。每个用户首先对公共流进行解码（将其他私有流视为噪声干扰)，成功解码后通过串行干扰抵消（SIC）去除公共流，然后再解码其自身的私有流（将其他用户私有流视作噪声）。这种架构使接收端能够**部分解码干扰、部分将干扰视为噪声**，从而在干扰管理上具有很大的灵活性。

&#x20;*图1：单层RSMA下行MISO系统架构示意图。基站（BS）将各用户消息拆分为公共部分和私有部分，分别编码成公共码流\$s\_{\mathcal{K}}\$（共享给所有\$K\$个用户）和用户\$k\$的私有码流\$s\_k\$。BS对公共流和各私有流进行线性预编码后同时发送给多个用户。每个用户首先解码公共流（包含所有用户的公共消息部分），成功解码并去除公共成分后，再解码只属于自己的私有流。*

在图1所示架构下，定义信道\$\mathbf{h}*k\$为用户\$k\$的\$N\_t\times 1\$信道向量（假设准静态平坦衰落信道模型）。设\$\mathbf{p}*c\$和\$\mathbf{p}*k\$分别为公共流和用户\$k\$私有流的预编码向量，则用户\$k\$处接收到的信号为
$y_k = \mathbf{h}_k^H (\mathbf{p}_c s_c + \sum_{i=1}^K \mathbf{p}_i s_i) + n_k,$
其中\$n\_k\$为噪声。对于用户\$k\$解码公共流\$s\_c\$，其信干噪比（SINR）为：
$\gamma_{c,k} = \frac{|\mathbf{h}_k^H \mathbf{p}_c|^2}{\sum_{j=1}^K |\mathbf{h}_k^H \mathbf{p}_j|^2 + \sigma^2},$
其中\$\sigma^2\$为噪声功率。同理，用户\$k\$在去除公共流后解码其私有流\$s\_k\$的SINR为：
$\gamma_{p,k} = \frac{|\mathbf{h}_k^H \mathbf{p}_k|^2}{\sum_{j\neq k}|\mathbf{h}_k^H \mathbf{p}_j|^2 + \sigma^2}.$
在理想解码条件下，以上SINR对应的**香农速率**为\$R*{c,k}=\log\_2(1+\gamma*{c,k})\$（用户\$k\$可支持的公共流速率）和\$R*{p,k}=\log\_2(1+\gamma\_{p,k})\$（用户\$k\$私有流速率）。实际采用有限块长编码时，可达速率将低于香农容量，本节后续将详细说明这一影响。每个用户\$k\$重构其完整消息\$W\_k\$需要从公共流中提取相应的公共部分\$W\_{c,k}\$并结合私有部分\$W\_{p,k}\$。因此，若公共流或私有流任一解码失败，都会导致用户\$k\$该时隙更新失败（其AoI不会降低）。为了保证每个用户都能正确接收公共流，公共流的编码速率\$R\_c\$必须不超过每个用户\$k\$对公共流的信道容量：即\$R\_c \le \log\_2(1+\gamma\_{c,k}),\ \forall k\$。换言之，\$R\_c\$受限于**最差信道用户**，通常设置\$R\_c = \min\_{k}\log\_2(1+\gamma\_{c,k})\$以满足所有用户的公共流解码要求。私有流则只需满足用户自身的容量限制：\$R\_{p,k} \le \log\_2(1+\gamma\_{p,k})\$。此外，为确保所有用户的基本服务质量（QoS），通常要求每个用户的*总速率*（公共部分 + 私有部分）不低于最低阈值\$r\_k^{\min}\$，即 \$R\_{c,k} + R\_{p,k} \ge r\_k^{\min}\$。

为了满足URLLC对于可靠性的苛刻要求，我们在模型中引入了*短包有限块长*传输下的**块错误率（BLER）**约束。不同于无限长分组时可以近似满足香农容量，有限块长\$n\$下信道编码的误码率\$\epsilon\$将明显升高，并导致有效速率损失。根据Polyanskiy等人的信息论结果，给定块长\$n\$和目标错误率\$\epsilon\$，信道的**可达速率**可近似表示为香农容量减去一个与\$\frac{1}{\sqrt{n}}\$成比例的修正项。例如对于AWGN信道，有公式近似：
$R(n,\epsilon) \approx C - \sqrt{\frac{V}{n}}\,Q^{-1}(1-\epsilon)\ln 2,$
其中\$C=\log\_2(1+\text{SNR})\$是容量，\$V\$是信道色散（由SNR确定的常数），\$Q^{-1}\$是高斯\$Q\$函数的反函数。该公式表明，在有限块长下为保证误码率\$\epsilon\$，速率必须留有裕量。对于一般慢衰落信道，上式需要取期望或考虑 outage 情况。在我们的模型中，将针对公共流和各私有流分别设置BLER约束，以确保每个用户的更新消息在短包传输下满足可靠性要求。例如，要求用户\$k\$对公共流的误块率\$\epsilon\_{c,k}\$和对私有流的误块率\$\epsilon\_{p,k}\$均不超过URLLC目标（如\$10^{-5}\$级别），即 \$\epsilon\_{c,k} \le \epsilon^{\text{max}},\ \epsilon\_{p,k}\le\epsilon^{\text{max}}\$。这些\$\epsilon\$将通过FBL理论与传输速率、块长和接收SINR关联起来，形成非线性约束。后文将给出具体的BLER计算表达式。

最后，考虑系统在*时隙*结构下传送更新信息。我们引入*用户调度*或*时隙分配*策略：即每个时隙决定哪一组用户发送更新，以及资源（如功率、码长）如何分配。RSMA可以在单个时隙同时为多个用户发送更新（通过公共流+多私有流），但鉴于功率和可靠性限制，在每个时隙可能不会为所有用户发送其更新。我们用指示变量\$c\_k(t)\in{0,1}\$表示时隙\$t\$是否为用户\$k\$发送更新（\$c\_k(t)=1\$表示发送）。也可以引入调度优先级策略，使得总是在AoI最高的用户中优先分配资源。总体而言，该系统的资源优化决策包括**功率分配**（公共流功率\$a\_c\$及各私有流功率\$a\_k\$）、**速率分配**（公共流总速率\$R\_c\$及各用户私有速率\$R\_{p,k}\$，相应地每个用户的公共部分速率\$R\_{c,k}\$）、**块长分配**（每个流采用的编码块长\$n\_c, n\_k\$等）、以及**用户调度/时隙分配**策略。下面我们将形式化地定义这些优化变量和约束条件。

## 优化变量与约束条件

### 优化变量

* **功率分配** \$(a\_c,{a\_k}*{k=1}^K)\$：将基站总发射功率\$P\_t\$在公共码流和各用户私有码流之间分配的比率或绝对值。功率分配直接影响各流的SINR和可靠性。令\$a\_c\$表示公共流功率，占比\$P\_c=a\_c P\_t\$，\$a\_k\$表示用户\$k\$私有流功率，占比\$P\_k=a\_k P\_t\$。为了满足功率预算，有约束 \$a\_c+\sum*{k=1}^K a\_k \le 1\$（或等于1，在此将功率归一化表示）。功率分配需要权衡公共流和私有流的重要性：增加公共流功率可以提高所有用户解码公共消息的可靠性，但会减少分配给私有消息的功率；反之亦然。

* **速率分配** \$(R\_c,{R\_{p,k}})\$：包括公共码流总速率\$R\_c\$和各用户私有码流速率\$R\_{p,k}\$，通常以比特每信道使用（bps/Hz）表示。公共流速率\$R\_c\$可以细分为各用户的公共消息部分\$R\_{c,k}\$，满足 \$R\_c = \sum\_{k=1}^K R\_{c,k}\$。每个用户的总服务速率\$R\_{k,\text{tot}} = R\_{c,k} + R\_{p,k}\$。速率分配决定了AoI更新的信息量和频率：更高的速率意味着在单位时间内传输更多更新数据，但给定功率和块长下更高速率会导致更高误码率。速率分配需要结合BLER约束来选择在有限块长下可实现的最大值。特别地，公共流速率\$R\_c\$受限于最弱用户的信道，如上节所述 \$R\_c \le \min\_k \log\_2(1+\gamma\_{c,k})\$，以保证所有用户可解码。各私有速率需满足 \$R\_{p,k} \le \log\_2(1+\gamma\_{p,k})\$。此外，还须满足每个用户的最低QoS速率 \$R\_{k,\text{tot}}\ge r\_k^{\min}\$。

* **块长分配** \$(n\_c,{n\_k})\$：公共码流和各私有码流的编码块长（即信道使用的符号数）。在短包通信中块长往往固定较小以控制时延，但我们也可以将不同长度视为可选优化参数。例如，可以在规定的块长集合\${n^{(1)},n^{(2)},...}\$中为每个流选择块长，或者在一定范围内连续优化\$n\$的值。块长影响BLER：更大的块长通常降低误码率（因为接近信道容量极限），但增加传输时延；反之，较小块长提高实时性但误码率上升。因此需要在块长上折中可靠性和时延。为限制时延，通常会有块长的上限约束 \$n\_c, n\_k \le n\_{\max}\$，或者规定所有流块长相同且等于系统时隙长度\$T\$符号等。本文假设块长可调且有已知上限。

* **用户调度/时隙分配策略** \${c\_k(t)}\$：这是一个离散决策变量集合，决定各时隙向哪些用户发送更新。简单起见，可设每时隙最多服务\$M\$个用户（\$1\le M \le K\$），其中\$M=K\$表示全体同时服务（RSMA支持并行发送公共+多私有流），\$M=1\$退化为时分策略。一种调度方案是在每个时隙选择用户子集\$\mathcal{U}(t)\subseteq{1..K}\$，对其发送更新，其余用户暂不发送，即\$c\_k(t)=1\$当\$k\in\mathcal{U}(t)\$，否则0。调度策略可以是固定的轮询、随机接入，或基于AoI的动态优先级（如总是选择当前AoI最大的用户优先更新）。该策略直接影响AoI演化，因为长时间未调度的用户AoI会线性增长。一些模型也允许调整每个用户在一个时隙内占用的时间比例，但在短包场景中我们多以时隙为基本单位，只决定是否发送即可。

### 约束条件

在上述变量下，我们建立如下主要约束条件：

* **总功率约束**：基站发射总功率不得超过\$P\_t\$。使用前述功率归一化变量表示即
  $a_c + \sum_{k=1}^K a_k \;\le\; 1,$
  该约束保证了功率分配策略在物理可实现范围内。在某些情况下也可采用等式（功率充分利用）或考虑功率最优分配时该约束自动满足的情况。

* **BLER可靠性约束**：这是URLLC场景的关键约束。利用FBL信道编码理论，可将每条流的发送速率、功率、块长与其块错率联系起来。具体来说，对于公共流，用户\$k\$解码公共码流的块错率\$\epsilon\_{c,k}\$是接收SINR \$\gamma\_{c,k}\$、块长\$n\_c\$和公共流码率\$R\_c\$的函数；类似地\$\epsilon\_{p,k}=f(\gamma\_{p,k}, n\_k, R\_{p,k})\$。一旦有解析近似公式，我们要求
  $\epsilon_{c,k} \le \epsilon^{\text{max}},\quad \epsilon_{p,k} \le \epsilon^{\text{max}},\qquad \forall k,$
  其中\$\epsilon^{\text{max}}\$是URLLC目标BLER（如\$10^{-5}\$）。上述\$\epsilon\$-约束可通过Polyanskiy公式等转化为对速率的约束。例如，在AWGN或准静态衰落信道下，可采用近似：
  $\epsilon_{p,k} \approx Q\!\Bigg(\frac{\sqrt{n_k}\left(\log_2(1+\gamma_{p,k}) - R_{p,k}\right)}{\sqrt{V_k}}\Bigg),$
  其中\$V\_k\$是对应信道的色散。如需\$\epsilon\_{p,k}\le\epsilon^{\max}\$，可近似得到 \$R\_{p,k} \le \log\_2(1+\gamma\_{p,k}) - \sqrt{\frac{V\_k}{n\_k}};Q^{-1}(1-\epsilon^{\max})\ln2\$（近似忽略高阶项）。公共流类似但需考虑\$R\_c\$和\$\gamma\_{c,k}\$。由于公共流对所有用户共享，必须满足对\$\forall k\$均成立，这通常是通过令\$R\_c\$不超过最弱用户的容量裕量来保证。上述公式只是示意，严格来说我们直接约束误块率计算公式本身。**Polyanskiy框架**提供了严格上下界，但为便于优化可使用其高斯近似形式。此外，在信道存在快衰落时，我们可将BLER理解为平均意义或 outage概率限制——例如要求 \$\Pr(\epsilon\_{p,k} \le \epsilon^{\max}) \ge 1-\delta\$ 等。

* **用户QoS约束**：为了保证基本服务，每个用户\$k\$的长期平均传输速率不低于某阈值\$r\_k^{\min}\$。在优化每时隙决策时，可等价施加瞬时速率下限 \$R\_{c,k}+R\_{p,k} \ge r\_k^{\min}\$（在所有送服务时隙满足；未服务时速率为0可不计入）。或者在随机调度下考虑平均约束：\$\mathbb{E}\[R\_{k,\text{tot}}] \ge r\_k^{\min}\$。QoS约束防止资源分配过度倾斜于某些用户导致“饿死”现象。

* **RSMA结构约束**：公共码流的速率和各用户公共部分速率间需满足 \$R\_c = \sum\_{k} R\_{c,k}\$，并且每个用户能够解码公共流：\$R\_c \le \log\_2(1+\gamma\_{c,k}),;\forall k\$。通常此约束通过引入辅助变量\$R\_{c,k}^{\text{dec}}=\log\_2(1+\gamma\_{c,k})\$并要求\$R\_c \le R\_{c,k}^{\text{dec}},;\forall k\$实现。此外，每个用户的消息流由公共和私有两部分组成，需要 \$R\_{k,\text{tot}} = R\_{c,k} + R\_{p,k}\$。这类约束保证了优化解是RSMA可实现的，并确保公共流在编码和解码上的一致性。

* **信道不确定性约束**：在CSI不完美的情况下，我们需要对上述速率、SINR约束进行健壮化处理。常见方法有两类：*最坏情况约束*和*概率约束*。最坏情况约束假设信道\$\mathbf{h}\_k\$落在一个不确定集（如\$|\Delta \mathbf{h}*k|\le \xi\$）内，优化时要求约束对该集内任何CSI扰动都成立。这通常导致更加保守的分配，例如降低速率上限或增加功率余量。概率约束（机会约束）则允许以一定小概率违约：例如\$\Pr*{\mathbf{h}*k}\[R\_c \le \log\_2(1+\gamma*{c,k})] \ge 1-\eta\$，确保大部分情况下可靠。在URLLC高可靠性要求下，机会约束中的违约概率\$\eta\$也需非常小。我们可以基于已知信道统计，利用Chernoff界等将概率约束转化为确定性约束。由于RSMA被证明对CSI不准确具有鲁棒性（其部分解码干扰策略在CSI不完美时仍优于传统方案），在模型中考虑信道不确定性有实际意义。简而言之，不确定CSI场景的建模需要在优化中纳入上述鲁棒/概率约束，以保证在实际信道偏差下依然满足BLER和QoS要求。

* **块长约束**：根据系统时隙长度和编解码复杂度，我们对块长施加限制。例如固定块长\$N\$或上限\$N\_{\max}\$：\$n\_c, n\_k \le N\_{\max}\$。若需要，将块长作为决策变量时还可假设只能取某些离散值集合。块长约束确保模型解不会选择不切实际的极大码长来降低误码率。一般而言，在时隙制下常采用统一的块长\$N\$用于所有流（或公共、私有流不同但各用户私有间相同），这样可以近似表示传输时延固定为一个时隙。

综上，我们建立了包含功率、速率、块长、调度等多维度的优化模型，并纳入了总功率、可靠性、QoS、RSMA架构一致性、信道不确定性、块长限制等约束条件。下一步定义优化目标函数——平均信息年龄，并给出其数学形式。

## 目标函数：平均信息年龄 (AoI)

**信息年龄AoI**定义为接收端最后一次成功接收到的新鲜信息以来经过的时间。对用户\$k\$而言，在时隙\$t\$的AoI记为\$\Delta\_k(t)\$，如果此时隙成功交付了该用户生成于时隙\$u\$的新信息，则\$\Delta\_k(t)\$会在接收时刻跳降至\$t-u\$（通常近似为一次单位时间下降），随后若一段时间没有再更新则AoI线性增长。简单来说，每当用户\$k\$获得一次新的更新，其AoI会“重置”为一个较小值，然后随着时间推移逐渐变大，直到下一次接收到更新时再度下降。**平均AoI**（Average AoI, AAoI）通常定义为长期平均\$\bar{\Delta}*k = \lim*{T\to\infty}\frac{1}{T}\sum\_{t=1}^T \Delta\_k(t)\$。在具有随机更新成功/失败的系统中，平均AoI可以通过更新过程的统计特性推导闭式表达式。例如，对于每时隙尝试发送且独立成功率为\$q\$的模型，更新到达按照几何分布，平均AoI可解析地表示为\$\bar{\Delta} = \frac{1}{q} + \frac{1-q}{2q}\$等形式（具体形式依赖于成功判定和采用的近似模型）。更一般地，AoI理论给出的平均AoI可由更新间隔的分布导出：若记相邻成功更新之间经历\$X\$个时隙（更新间隔随机变量），则有公式
$\bar{\Delta} = \frac{\mathbb{E}[X^2]}{2\,\mathbb{E}[X]} + \frac{\mathbb{E}[X]}{2}:contentReference[oaicite:49]{index=49},$
这一结果也被广泛用于队列模型的AoI分析。其中\$\mathbb{E}\[X]\$是平均更新间隔（例如几何分布情形\$\mathbb{E}\[X]=1/q\$）。如果成功概率或更新间隔受资源分配影响，我们需将其作为决策变量的函数纳入优化。

在本报告所述RSMA系统中，每个用户\$k\$的AoI演化受到调度和可靠性双重影响：只有当用户\$k\$在某一时隙被调度且其公共流和私有流都成功解码时，该用户AoI才会在该时隙后下降至一个较小值，否则AoI将继续增加1。对于给定的调度策略和物理层资源分配，可以计算每个用户的平均AoI。我们的目标是**最小化所有用户平均AoI的加权和**，常见地可取等权，即最小化 \$\frac{1}{K}\sum\_{k=1}^K \bar{\Delta}\_k\$（所有用户平均AoI）以提升整体信息及时性。如果关注特定关键用户，也可给予不同权重。需要强调的是，AoI不同于传统吞吐量或时延指标，它取决于**更新频率**和**成功率**的综合作用：增大发送频度可以降低AoI，但如果由此导致更多失败（降低成功率），反而可能恶化AoI。因此在资源有限和URLLC约束条件下，需要优化地**折中发送频率与可靠性**才能有效降低AoI。

本问题的优化目标函数可以表述为：
$ \min_{\{a_c,a_k,R_c,R_{p,k},n_c,n_k,c_k(t)\}} \; \frac{1}{K}\sum_{k=1}^K \bar{\Delta}_k,$
其中\$\bar{\Delta}\_k\$是上述变量决策下用户\$k\$的长期平均AoI。由于直接优化AoI较复杂，我们也可以使用AoI的解析表达式将其转化为决策变量的函数。例如，在周期性更新策略和独立错误情况下，\$\bar{\Delta}\_k\$可表示为时隙调度间隔和BLER的函数，并将其代入目标。例如假设用户\$k\$被调度的平均间隔为\$T\_k\$时隙且每次成功概率\$1-\epsilon\_k\$，则更新间隔\$X\_k\$平均为\$T\_k/(1-\epsilon\_k)\$，由此估计\$\bar{\Delta}\_k \approx \frac{T\_k}{2(1-\epsilon\_k)} + \frac{1}{2(1-\epsilon\_k)}\$等。更严格的推导可参考文献中的闭式结果。在后续附录中，我们将给出一个简单场景下AoI计算的推导公式作为参考。

## 不同CSI假设下的模型设定

在资源优化模型中，信道状态信息（CSI）的获取程度对问题设定有重要影响。本文考虑两种典型场景：(1) **确定性CSI模型**，即假设基站能精确获取所有用户的CSI（也称完美CSI），(2) **不确定CSI模型**，即CSI存在误差或变化，仅已知统计或区间信息，需要采用鲁棒/概率约束设计。下面分别讨论这两种模型设定的特点。

### 场景一：完美CSI（确定性模型）

在此设定下，基站在优化时能够精确地知道所有用户信道向量\$\mathbf{h}*k\$，因此各用户SINR表达式和容量约束都是确定性的。模型中不需要考虑额外的CSI不确定约束，优化问题可视为典型的**确定性非凸优化**。具体而言，优化变量包括前述功率、速率、块长、调度变量，约束包括总功率、BLER、QoS等，均按给定CSI计算。此时，公共流容量限制 \$R\_c \le \log\_2(1+\gamma*{c,k})\$、私有流容量 \$R\_{p,k} \le \log\_2(1+\gamma\_{p,k})\$ 等均可直接套用，因为\$\gamma\$已知。完美CSI模型一般作为理想性能上界，用于评估在无CSI误差时系统的AoI最优表现。

需要注意的是，即使CSI已知，该优化问题依然相当复杂：(i) 目标函数平均AoI本身是非线性/非凸函数，难以直接优化；(ii) BLER约束由Q函数等非凸形式表示；(iii) 调度变量\$c\_k(t)\$为0-1整数，带来组合法复杂度。因此，这属于一个带混合离散变量的非凸约束优化问题。在完美CSI设定下，我们倾向于首先忽略调度的离散性，假设持续服务所有用户或采用频率分割等，使问题主要关注功率和速率分配；然后再通过启发式或轮流优化纳入调度因素。

### 场景二：不完美CSI（不确定性模型）

在实际系统中，基站对CSI的了解可能不完美，例如由于时变衰落和估计误差，常用模型是假设\$\mathbf{h}\_k\$可表示为\$\hat{\mathbf{h}}\_k + \Delta \mathbf{h}\_k\$，其中\$\hat{\mathbf{h}}\_k\$是估计值，\$\Delta \mathbf{h}\_k\$是未知误差。此时需要将鲁棒性纳入资源分配决策。常见处理方法之一是**最坏情况优化**，即假设\$|\Delta \mathbf{h}*k|\$在已知范围内，并要求在误差范围内所有可能真实信道下都满足QoS和可靠性约束。例如，将容量约束改为 \$R\_c \le \min*{|\Delta \mathbf{h}*k|\le\xi} \log\_2(1+\gamma*{c,k}(\mathbf{h}*k))\$，确保无论真实信道如何偏移，公共流都能被所有用户解码。这种方法通常导致保守的分配，但提供了严格的可靠性保证。另一方法是**机会约束优化**，利用CSI统计特性，允许小概率的约束违约。例如改为 \$\Pr*{\Delta \mathbf{h}*k}\[R\_c \le \log\_2(1+\gamma*{c,k})] \ge 1-\eta\$。在URLLC高可靠性场景，我们需令\$\eta\$极小（如\$<10^{-5}\$）确保几乎确定满足。机会约束常通过**等价平均约束**或**分位数约束**转化。

不确定CSI模型会显著增加问题复杂度，因为约束本身成为随机或区间约束。为求解此类问题，可采用**随机优化**或**鲁棒优化**框架，将约束松弛或转化后使用迭代方法求解。例如，通过引入拉格朗日乘子和伯恩斯坦不等式将概率约束近似为确定形式，或者采用样本平均近似（SAA）方法，将问题转化为一系列采样CSI下的确定优化。也可针对最坏情况约束采用双层优化（内层寻找最不利CSI，外层优化资源），可借助凸近似和下降算法。值得一提的是，RSMA相较传统策略对CSI不准确更具鲁棒性——公共流承载公共信息，在CSI不准时仍可由弱用户可靠解码，而私有流在预编码设计时也可考虑统计CSI以减少干扰。因此，在不完美CSI场景下，RSMA的资源优化依然有望保持较好的AoI性能和可靠性，只是需要在模型中加入上述鲁棒约束，使解法略为保守从而预留抗误差裕度。

总之，两种CSI设定下模型的主要区别在于约束处理：完美CSI时约束为确定形式，不确定CSI时需加入鲁棒/概率约束。但无论哪种情形，我们的优化目标（平均AoI最小化）保持不变，都需要在功率、速率、块长和调度上找到最佳分配策略，以在满足URLLC可靠性和QoS前提下尽可能降低AoI。

## 求解方法建议

综合考虑上述资源优化问题的特征——目标函数复杂非凸、约束多且有离散变量——求解需要结合凸优化近似和智能算法相结合的策略。以下列出两类可行的求解思路：

**1. 基于优化分解和凸近似的方法：**
利用优化理论，可尝试将问题分解为多个子问题或采用迭代近似的方法求解。例如：

* *半定松弛（SDR）+凸凹程序（CCCP）：* 对于涉及波束成形的MISO场景，常用**半定松弛**将原非凸的秩1约束放宽（忽略波束协方差矩阵的秩限制），先求得一个连续解近似。接下来应用\*\*凸-凹程序(CCCP)**或连续迭代的方法处理AoI和BLER等非凸部分：将非凸函数表示为差分形式，线性化凹部分迭代优化。例如，将\$Q^{-1}\$函数用凸上界逼近，将AoI表达式用历史解线性展开。通过交替更新功率、速率等，可以趋近局部最优解。类似思想的**逐次凸近似 (SCA)\*\*算法已成功用于RSMA有限块长场景的和速率最大化。我们可借鉴其步骤：首先固定其它变量优化功率分配（凸问题），再固定功率优化速率分配，同时逐次逼近BLER约束。每轮迭代后检查约束收敛和目标改善，直至收敛于某个局部最优解。这种方法能在多变量耦合的情况下通过分块优化降低难度。

* *交替优化 (AO) 与调度分离:* 面对离散调度变量\$c\_k(t)\$，可采用**交替优化**策略：在给定调度下优化功率/速率/块长连续变量，再基于当前AoI状态优化调度决策。调度优化可采用启发式算法，如基于AoI最大优先或解决一个加权覆盖问题。也可以通过**整数线性规划**求解小规模调度。为了降低复杂度，也可以将调度问题Relax成连续变量（如允许0\~1之间)然后取阈值近似。

* *元启发式算法:* 对于较难的非凸优化，也可以使用粒子群优化(PSO)、遗传算法(GA)等**随机搜索/进化算法**来获得近优解。例如，文献中通过PSO优化了RSMA系统的静态功率分配以降低AoI。这类算法不依赖梯度信息，适合复杂黑箱目标，但收敛速度相对慢。实际应用中，可将PSO用于生成初始种群或初解，然后结合梯度方法精 Refinement。

**2. 基于强化学习和智能决策的方法：**
由于AoI反映的是一个**时间平均性能**，可以将问题建模为一个马尔可夫决策过程 (MDP)，在每个时隙根据状态选择动作（调度/功率分配），以长期AoI为回报优化目标。强化学习 (RL)
