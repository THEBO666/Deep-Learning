# AoI-Aware Interference Mitigation in Multi-Cell NOMA Networks: Problem Modeling and Solution Summary

本节详细总结论文《AoI-Aware Interference Mitigation for Task-Oriented Multicasting in Multi-Cell NOMA Networks》中的建模与求解策略，涵盖信道模型、AoI模型、多目标优化问题及其分解求解方法。

---

## 一、系统与信道建模

### 1. 系统结构

* 多小区下行 NOMA 系统。
* 基站集合：$b \in \mathcal{B} = \{1, 2, \dots, B\}$。
* 每个 BS 向多组设备群发 $M$ 条多播消息，使用 NOMA（功率叠加）方案。
* 每组设备：$G_m^b$，其中每组含有 $N$ 个 IoT 设备。

### 2. 接收信号模型

$$
y = h_m^b \left( \sum_{i=1}^m \sqrt{p_i^b} \cdot m_i \right) + Z
$$

* $h_m^b$：组中最差设备的 CSI。
* $Z$：加性高斯白噪声 $Z \sim \mathcal{N}(0, \sigma^2)$。

### 3. 可达速率模型

$$
R_{m \rightarrow m}^b = \log_2\left(1 + \frac{|h_m^b|^2 p_m^b}{I_m^b + \psi_m^b + Z} \right)
$$

* 组内干扰：$I_m^b = |h_m^b|^2 \sum_{\ell = m+1}^k p_\ell^b$
* 小区间干扰：$\psi_m^b = \sum_{b' \neq b} \sum_{i=1}^M |h_i^{b'}|^2 p_i^{b'}$

---

## 二、AoI 建模

### 1. AoI 演化模型

$$
\Delta(t+1) = \begin{cases}
1, & \text{成功解码} \\
\Delta(t) + 1, & \text{失败或未调度}
\end{cases}
$$

设备 $U_{m,n}^b$ 的 AoI 更新公式：

$$
\Delta_{m,n}^b(t+1) = \begin{cases}
1, & \mathbb{I}_{m,n}(S^b \circ D^b) = 1 \\
\Delta_{m,n}^b(t) + 1, & \text{否则}
\end{cases}
$$

### 2. AoI 聚合指标

* Sum AoI：$\Delta_S = \sum_{j=1}^{N} \Delta_j$
* Peak AoI：$\Delta_P = \max_j \Delta_j$
* Average AoI：$\Delta_A = \frac{1}{N} \sum_{j=1}^N \Delta_j$

采用 Sum AoI 更能反映组信息的新鲜度。

### 3. 加权 AoI 目标函数

$$
\Delta^b(t) = \sum_{i=1}^M \sum_{j=1}^N w_{m_i}^b \cdot \Delta_{m_i,n_j}^b(t)
$$

---

## 三、优化问题建模

### 总目标函数：

$$
\text{P1:} \quad \max \sum_{b=1}^B \left( C^b(t) - \Delta^b(t) \right)
$$

约束：

$$
\sum_{m=1}^M p_m^b(t) \leq 1, \quad \forall b, m
$$

目标是：

1. 提高成功传输组数 $C^b(t)$
2. 降低加权 AoI $\Delta^b(t)$

---

## 四、AIM 算法设计

### 1. 分组策略：MAB + $\varepsilon$-greedy

* 建模为多臂赌博机问题。
* 每次选择组大小 $a \in A$，对设备进行重分组。
* 奖励：

$$
R^b_a(t) = C^b(t) - \Delta^b(t) - Pen(t)
$$

* 惩罚项：$Pen(t) = p(|m_c^b|)$

$\varepsilon$-greedy 策略：

$$
a^* = \begin{cases}
\arg\max_a Q_a(t), & 1 - \varepsilon(t) \\
\text{随机选择}, & \varepsilon(t)
\end{cases}
$$

更新：

$$
Q_{a^*}(t) = Q_{a^*}(t - 1) + \frac{1}{J_{a^*}(t)} [R_{\text{sum}} - Q_{a^*}(t-1)]
$$

### 2. 调度策略：优先调度 cell-edge 且 AoI 高的组

* 固定调度边缘组 $m_c^b$。
* 非边缘组 $m_{nc}^b$ 中选择前 $U - |m_c^b|$ 个 AoI 最大组。

调度上限由功率最小值反推：

$$
V_\phi (U^2 - U) \leq 2 \Rightarrow U \leq \text{解析解}
$$

### 3. 功率分配策略：启发式线性递减

* Cell-edge：优先分配，份额为 $V_c$，其余为 $V_{nc} = 1 - V_c$。
* 功率分配：

$$
p_{mj}^b \geq p_\phi^b - j V_\phi
$$

* 其中 $p_\phi^b = \min(V_\phi \cdot |m_c^b|, V_c)$

---

## 五、优化性质

* **P2（MAB）为 NP-hard**：等价于多子集和最大化问题；
* **调度策略**在组数上限下 **最小化 AoI 最优**；
* **功率分配策略**中调度组数上限 $U$ 可解析计算。

---

如需进一步总结 AIM 的仿真性能、参数影响分析、GA 对比等内容，请告知。
