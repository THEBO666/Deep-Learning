RSMA私有流精编码方法详解
引言
速率分割多址（RSMA）是一种先进的无线通信技术，通过将消息分为公共流和私有流来提高系统性能。在RSMA中，私有流仅由目标用户解码，其他用户将其视为噪声，因此需要精心设计的精编码器来减少干扰并优化传输效率。本文详细分析了文献中提到的三种线性精编码方法——零强制波束成形（ZFBF）、正则化零强制/最小均方误差（RZF/MMSE）和块对角化（BD）——在RSMA私有流中的应用，包括它们的定义、公式推导和在不完美CSIT（发送端信道状态信息）下的优化策略。
RSMA中的私有流
在RSMA系统中，传输信号由公共流 ( s_c ) 和 ( K ) 个私有流 ( s_1, s_2, \dots, s_K ) 组成。公共流由所有用户解码，而私有流 ( s_k ) 仅由用户 ( k ) 解码，其他用户将其视为噪声。信号模型可以表示为： [ \mathbf{x} = \mathbf{p}c s_c + \sum{k=1}^K \mathbf{p}_k s_k ] 其中：

( \mathbf{p}_c ) 是公共流的精编码器。
( \mathbf{p}_k ) 是私有流 ( s_k ) 的精编码器。
( s_c ) 和 ( s_k ) 分别是公共流和私有流的数据符号。

私有流的精编码器 ( \mathbf{p}_k ) 通常分解为方向和功率： [ \mathbf{p}_k = \sqrt{P_k} \tilde{\mathbf{p}}_k ] 其中：

( \tilde{\mathbf{p}}_k = \frac{\mathbf{p}_k}{|\mathbf{p}_k|} ) 是单位方向向量。
( P_k = |\mathbf{p}_k|^2 ) 是分配给私有流 ( k ) 的功率。

目标是通过设计 ( \tilde{\mathbf{p}}_k ) 和 ( P_k )，最大化用户 ( k ) 的接收速率，同时最小化对其他用户的干扰。
精编码方法详解
1. 零强制波束成形（ZFBF）
定义
ZFBF是一种线性精编码技术，旨在通过将精编码器设计在其他用户信道的零空间（null space）中，完全消除多用户干扰。ZFBF适用于发送端天线数 ( M ) 大于或等于用户数 ( K ) 的场景。
公式推导
假设信道矩阵为 ( \mathbf{H} = [\mathbf{h}_1, \mathbf{h}_2, \dots, \mathbf{h}_K] )，其中 ( \mathbf{h}_k \in \mathbb{C}^{M \times 1} ) 是用户 ( k ) 的信道向量。对于用户 ( i ) 的私有流，ZFBF精编码器 ( \mathbf{p}_i ) 需要满足： [ \mathbf{h}_j^H \mathbf{p}_i = 0, \quad \forall j \neq i ] 这意味着用户 ( i ) 的信号在其他用户 ( j ) 的接收端无干扰。
构造包含除用户 ( i ) 外的所有用户信道的矩阵： [ \mathbf{H}{-i} = [\mathbf{h}1, \dots, \mathbf{h}{i-1}, \mathbf{h}{i+1}, \dots, \mathbf{h}K] ] ZFBF精编码器 ( \mathbf{p}i ) 通过计算 ( \mathbf{H}{-i} ) 的伪逆得到： [ \mathbf{p}i = \mathbf{H}{-i}^\dagger (\mathbf{H}{-i} \mathbf{H}_{-i}^\dagger)^{-1} \mathbf{e}_i ] 其中：

( \mathbf{H}{-i}^\dagger ) 是 ( \mathbf{H}{-i} ) 的伪逆。
( \mathbf{e}_i ) 是选择向量（例如，第 ( i ) 个单位向量）。
伪逆确保 ( \mathbf{p}i ) 位于 ( \mathbf{H}{-i} ) 的零空间中。

为满足功率约束，归一化精编码器： [ \tilde{\mathbf{p}}_i = \frac{\mathbf{p}_i}{|\mathbf{p}_i|}, \quad \mathbf{p}_i = \sqrt{P_i} \tilde{\mathbf{p}}_i ] 其中 ( P_i ) 是分配给私有流 ( i ) 的功率。
在RSMA中的应用
在RSMA中，ZFBF用于设计私有流的精-encoding方向 ( \tilde{\mathbf{p}}_i )，以确保私有流 ( s_i ) 不干扰其他用户。私有流的其他用户干扰被视为噪声，而公共流 ( s_c ) 的精-encoding ( \mathbf{p}_c ) 则采用多播方式设计。
优点与局限性

优点：完全消除多用户干扰，适用于信道条件良好且天线数充足的场景。
局限性：当 ( M < K ) 或信道矩阵条件数差时，ZFBF可能导致噪声放大或不可行。

2. 正则化零强制/最小均方误差（RZF/MMSE）
定义
RZF（也称为MMSE精编码）通过在ZFBF的基础上引入正则化项，平衡干扰抑制和噪声增强。RZF适用于信道条件不佳或系统过载（( M < K )）的场景。
公式推导
RZF精编码器的矩阵形式为： [ \mathbf{P} = \mathbf{H}^H (\mathbf{H} \mathbf{H}^H + \alpha \mathbf{I})^{-1} ] 其中：

( \mathbf{H} \in \mathbb{C}^{M \times K} ) 是所有用户的信道矩阵。
( \alpha ) 是正则化参数，通常设置为噪声方差 ( \sigma^2 ) 或与用户数和信噪比相关。
( \mathbf{I} ) 是单位矩阵。

对于用户 ( i ) 的私有流，精-encoding器 ( \mathbf{p}_i ) 是 ( \mathbf{P} ) 的第 ( i ) 列： [ \mathbf{p}_i = \mathbf{H}^H (\mathbf{H} \mathbf{H}^H + \alpha \mathbf{I})^{-1} \mathbf{e}_i ] 归一化后： [ \tilde{\mathbf{p}}_i = \frac{\mathbf{p}_i}{|\mathbf{p}_i|}, \quad \mathbf{p}_i = \sqrt{P_i} \tilde{\mathbf{p}}_i ]
RZF的推导基于最小均方误差（MMSE）准则，目标是最小化发送信号和接收信号之间的误差： [ \min_{\mathbf{P}} \mathbb{E} \left[ |\mathbf{s} - \mathbf{H}^部分倒挂的MMSE精-encoding可以通过优化以下目标函数得到： [ J = \mathbb{E} \left[ |\mathbf{s} - \mathbf{H}^H \mathbf{y}|^2 \right] + \alpha |\mathbf{P}|^2 ] 其中：

( \mathbf{s} ) 是发送信号。
( \mathbf{y} ) 是接收信号。
( \alpha ) 控制正则化强度。

通过求解 ( \frac{\partial J}{\partial \mathbf{P}} = 0 )，得到上述RZF精-encoding公式。
在RSMA中的应用
在RSMA中，RZF用于设计私有流的精-encoding方向 ( \tilde{\mathbf{p}}_i )，通过正则化项平衡干扰和噪声，特别适合不完美CSIT场景。
优点与局限性

优点：在信道条件差或过载系统中有更好的鲁棒性，性能接近局部最优。
局限性：需要选择合适的 ( \alpha )，计算复杂度高于ZFBF。

3. 块对角化（BD）
定义
BD是一种针对多用户MIMO系统的精-encoding方法，适用于每个用户有多个数据流的情况。BD通过设计精-encoding器，使有效信道呈块对角形式，消除用户间干扰。
公式推导
假设用户 ( i ) 有 ( d_i ) 个数据流，精-encoding器矩阵 ( \mathbf{P}_i \in \mathbb{C}^{M \times d_i} ) 需要满足： [ \mathbf{H}_j \mathbf{P}_i = 0, \quad \forall j \neq i ] 其中 ( \mathbf{H}_j \in \mathbb{C}^{N_j \times M} ) 是用户 ( j ) 的信道矩阵。
构造包含除用户 ( i ) 外的所有用户信道的矩阵： [ \mathbf{H}{-i} = [\mathbf{H}1^T, \dots, \mathbf{H}{i-1}^T, \mathbf{H}{i+1}^T, \dots, \mathbf{H}_K^T]^T ] BD精-encoding器 ( \mathbf{P}i ) 通过计算 ( \mathbf{H}{-i} ) 的零空间得到： [ \mathbf{P}_i = \mathbf{U}_i \mathbf{V}_i ] 其中：

( \mathbf{U}i ) 是 ( \mathbf{H}{-i} ) 零空间的基向量矩阵。
( \mathbf{V}_i \in \mathbb{C}^{d_i \times d_i} ) 是任意矩阵，通常通过SVD优化。

功率分配后： [ \mathbf{P}_i = \sqrt{P_i} \frac{\mathbf{P}_i}{|\mathbf{P}_i|_F} ] 其中 ( |\cdot|_F ) 是Frobenius范数。
在RSMA中的应用
在RSMA中，BD用于设计私有流的精-encoding器 ( \mathbf{P}_i )，确保每个用户的私有流不干扰其他用户，适用于多数据流场景。
优点与局限性

优点：支持多数据流，消除用户间干扰。
局限性：需要更多发送天线（( M \geq \sum_{j \neq i} N_j )），计算复杂度较高。

在RSMA中的优化框架
在RSMA中，精-encoding优化通常涉及以下目标：

加权和速率（WSR）：( \max \sum_{k=1}^K w_k (R_k + C_k) )
最大公平速率（MMF-rate）：( \max \min_{k \in \mathcal{K}} (R_k + C_k) )
能效（EE）：( \max \frac{\sum_{k=1}^K (R_k + C_k)}{\eta \text{tr}(\mathbf{P}\mathbf{P}^H) + P_{\text{cir}}} )
总功率最小化：( \min \text{tr}(\mathbf{P}\mathbf{P}^H) )

这些优化问题通常是非凸的，需要使用如加权最小均方误差（WMMSE）、逐步凸近似（SCA）等算法求解。
在不完美CSIT下，精-encoding设计需要考虑信道估计误差，采用鲁棒优化方法，如最坏情况优化或长效优化。
研究现状与争议

研究进展：文献表明，RSMA通过速率分割比传统SDMA和NOMA更具优势，尤其在不完美CSIT下。ZFBF、RZF/MMSE和BD等方法已被广泛研究，用于私有流精-encoding设计。
争议：多载波RSMA的精-encoding优化仍处于初期，联合优化子载波分配、精-encoding设计和功率分配的计算复杂度高，实时性问题尚未解决。
未来方向：开发低复杂度的实时算法，探索机器学习方法（如深度强化学习）以优化精-encoding设计。

总结
RSMA中的私有流精-encoding设计是提高系统性能的关键。ZFBF通过完全消除干扰适用于信道条件良好的场景；RZF/MMSE通过正则化平衡干扰和噪声，适合复杂环境；BD支持多数据流，确保用户间无干扰。这些方法通过优化精-encoding方向 ( \tilde{\mathbf{p}}_i ) 和功率 ( P_i )，在RSMA中实现高效的私有流传输。
精-encoding方法比较表



方法
目标
公式
适用场景
优点
局限性



ZFBF
完全消除干扰
( \mathbf{p}i = \mathbf{H}{-i}^\dagger (\mathbf{H}{-i} \mathbf{H}{-i}^\dagger)^{-1} \mathbf{e}_i )
( M \geq K )，信道条件好
简单，完全消除干扰
噪声放大，需 ( M \geq K )


RZF/MMSE
平衡干扰和噪声
( \mathbf{P} = \mathbf{H}^H (\mathbf{H} \mathbf{H}^H + \alpha \mathbf{I})^{-1} )
信道条件差，过载系统
鲁棒性强，性能接近最优
需选择 ( \alpha )，复杂度高


BD
消除用户间干扰（多数据流）
( \mathbf{P}_i = \mathbf{U}_i \mathbf{V}_i )
多数据流，( M \geq \sum N_j )
支持多数据流，无用户间干扰
需更多天线，复杂度高


